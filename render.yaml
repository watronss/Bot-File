services:
  - type: web
    name: hotmail-bot-panel
    env: python
    plan: free
    buildCommand: |
      pip install -r requirements.txt
      mkdir -p uploads templates logs data
    startCommand: python app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
